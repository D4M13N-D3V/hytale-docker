# Hytale Server Configuration
# Copy this file to .env and customize as needed

# ============================================================
# Java / JVM Settings
# ============================================================

# Memory allocation for the JVM (default: 8G)
# Recommended: 4G minimum, 8G+ for larger servers
JAVA_MEMORY=8G

# ============================================================
# Server Settings
# ============================================================

# UDP port for the server (default: 5520)
# Hytale uses QUIC protocol over UDP
SERVER_PORT=5520

# Authentication mode: authenticated or offline (default: authenticated)
# Use 'offline' for LAN play or testing without authentication
AUTH_MODE=authenticated

# Patchline: release or pre-release (default: release)
# Use 'pre-release' to test upcoming features
PATCHLINE=release

# ============================================================
# Backup Settings
# ============================================================

# Enable automatic backups (default: false)
ENABLE_BACKUP=false

# Backup frequency in minutes (default: 30)
BACKUP_FREQUENCY=30

# ============================================================
# Authentication Tokens (Optional)
# ============================================================
# These are OPTIONAL for automated/headless deployments.
#
# Without tokens:
#   - The server will auto-trigger 'auth login browser' on boot
#   - You'll need to complete OAuth auth via the URL in the logs
#   - Auth needs to be completed each restart (tokens expire ~1 hour)
#
# With tokens:
#   - Server starts fully authenticated
#   - Use for automated/production deployments
#   - Obtain via Server Provider Authentication API
#   - See: https://support.hytale.com/hc/en-us/articles/45328341414043

# Session token for server authentication (optional)
HYTALE_SERVER_SESSION_TOKEN=

# Identity token for server authentication (optional)
HYTALE_SERVER_IDENTITY_TOKEN=

# ============================================================
# Playit.gg Tunnel (Optional)
# ============================================================
# Enable playit.gg for external access without port forwarding
# To use: docker-compose --profile playit up -d
#
# Get your secret key from: https://playit.gg/account/tunnels

# Playit.gg agent secret key
PLAYIT_SECRET_KEY=
